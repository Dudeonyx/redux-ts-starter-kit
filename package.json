{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "bootstrap": "lerna bootstrap",
    "clean": "rimraf packages/**/lib",
    "build": "yarn clean && tsc -b packages/slice packages/core",
    "publish": "yarn clean && lerna publish"
  },

  "devDependencies": {
    "@types/jest": "^23.3.11",
    "@types/node": "^10.12.18",
    "@types/redux-immutable-state-invariant": "^2.1.0",
    "babel-core": "^6.0.0 || ^7.0.0-0",
    "babel-jest": "^23.0.0 || ^24.0.0",
    "husky": "^1.3.1",
    "jest": "^23.6.0",
    "lint-staged": "^8.1.0",
    "prettier": "1.15.3",
    "rimraf": "^2.6.3",
    "lerna": "^3.8.4",
    "typescript": "^3.2.2"
  },
  "workspaces": ["packages/*"]
}
